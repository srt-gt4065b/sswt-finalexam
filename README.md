# 📊 통합 시험 시스템 사용 가이드

## 🎯 개요

**index.html** 과 **admin.html** 두 개의 파일로 통합된 온라인 시험 시스템입니다.

---

## 📁 파일 구조

```
integrated-exam-system/
├── index.html          # 학생용 시험 페이지
├── admin.html          # 관리자 페이지 (교수용)
└── README.md           # 이 파일
```

---

## 🔥 Firebase 설정 (필수!)

### 1단계: Firebase Config 복사

1. Firebase Console 접속: https://console.firebase.google.com/
2. 프로젝트 설정 → SDK 설정 및 구성
3. firebaseConfig 복사

### 2단계: 파일 수정

**index.html 파일 열기** (약 30번째 줄):
```javascript
const firebaseConfig = {
    apiKey: "실제_API_키로_교체",
    authDomain: "실제_도메인으로_교체",
    projectId: "실제_프로젝트ID로_교체",
    storageBucket: "실제_스토리지로_교체",
    messagingSenderId: "실제_ID로_교체",
    appId: "실제_앱ID로_교체"
};
```

**admin.html 파일 열기** (약 195번째 줄):
```javascript
// 위와 동일한 설정 입력
const firebaseConfig = {
    apiKey: "실제_API_키로_교체",
    authDomain: "실제_도메인으로_교체",
    projectId: "실제_프로젝트ID로_교체",
    storageBucket: "실제_스토리지로_교체",
    messagingSenderId: "실제_ID로_교체",
    appId: "실제_앱ID로_교체"
};
```

**중요**: 두 파일 모두 같은 Firebase 설정을 사용해야 합니다!

### 3단계: Firestore 보안 규칙 설정

Firebase Console → Firestore Database → 규칙:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 12, 31);
    }
  }
}
```

---

## 👨‍🏫 관리자 사용법 (admin.html)

🎉 admin.html V3 완성!
포함된 모든 기능:

🔐 관리자 비밀번호 보호

📥 CSV 업로드 + 미리보기

🗃 버전 관리 + JSON 백업

⚙ 주제별·난이도별 랜덤 문제 생성

🚫 중복 문제 자동 제거

✔ 선택된 문제만 Firestore 업로드

🧮 정답은 Firestore에만 저장 → 학생에게는 숨김

Tailwind UI 전체 적용

### 2️⃣ 문제 업로드 (CSV)

#### CSV 파일 형식:

필수 컬럼 (순서대로):
1. **questionId** - 문제 번호 (예: Q1, Q2)
2. **type** - calculation, short, essay, interpretation, chart
3. **questionText** - 문제 내용
4. **points** - 배점 (숫자)
5. **answerKey** - 정답 또는 채점 기준
6. **useStudentData** - true/false
7. **chartType** - histogram, scatter, boxplot, residual (선택)
8. **difficulty** - easy, medium, hard

#### CSV 예시:

```csv
questionId,type,questionText,points,answerKey,useStudentData,chartType,difficulty
Q1,calculation,"당신의 평균 점수 {avgScore}를 반올림하면?",10,"{avgScore}",true,,easy
Q2,chart,"히스토그램을 보고 정규성을 판단하시오.",20,"정규성 판단",true,histogram,hard
```

#### 업로드 절차:

1. CSV 파일 준비
2. "파일 선택" 버튼 클릭
3. CSV 파일 선택
4. "문제 업로드" 버튼 클릭
5. 성공 메시지 확인

### 3️⃣ 업로드된 문제 확인

- "새로고침" 버튼으로 최신 문제 확인
- 문제 개수, 시험 시간, 최종 업데이트 시간 표시

### 4️⃣ 학생 응답 조회

1. 하단 "전체 응답 보기" 버튼 클릭
2. 제출된 답안 목록 확인:
   - 학번, 이름, 모드, 제출 시간, 소요 시간

---

## 👨‍🎓 학생 사용법 (index.html)

### 1️⃣ 로그인

1. 이름 입력
2. 학번 입력
3. "연습 모드 시작" 또는 "실전 시험 시작" 선택

### 2️⃣ 시험 응시

- 상단에 남은 시간 표시
- 각 문제에 답안 작성
- "시험 제출하기" 버튼으로 제출

### 3️⃣ 제한사항

- **연습 모드**: 무제한 재응시 가능
- **실전 시험**: 1회만 응시 가능 (학번 기준)

---

## 🎨 개인화 기능

### 학생별 고유 데이터

학번을 기반으로 각 학생마다 다른 데이터 생성:

- **Galaga 게임 점수** 20회 플레이 기록
- **통계값**: 평균, 표준편차, 최고점, 최저점
- **난수**: randomNum1, randomNum2, randomNum3, randomPValue

### 사용 가능한 변수

문제 텍스트에서 중괄호로 사용:

```
{maxScore}       - 최고 점수
{minScore}       - 최저 점수
{avgScore}       - 평균 점수
{stdDev}         - 표준편차
{first5Avg}      - 처음 5회 평균
{last5Avg}       - 마지막 5회 평균
{first5Scores}   - 처음 5회 점수 목록
{last5Scores}    - 마지막 5회 점수 목록
{randomNum1}     - 난수 (600-900)
{randomNum2}     - 난수 (150-250)
{randomNum3}     - 난수 (20-70)
{randomPValue}   - p-value (0.000-0.100)
```

### 차트 자동 생성

chartType 지정 시 학생 데이터로 차트 자동 생성:

- **histogram** - 히스토그램 (점수 분포)
- **scatter** - 산점도 (시도별 점수)
- **boxplot** - 박스플롯 (분포 특성)
- **residual** - 잔차 플롯 (회귀 분석)

---

## 🔒 Firebase 데이터 구조

### questions 컬렉션

```
questions/
  ├── practice/
  │   ├── questions: [문제 배열]
  │   ├── timeLimit: 600 (초 단위)
  │   ├── updatedAt: "2025-01-15T10:30:00Z"
  │   └── count: 10
  └── real/
      ├── questions: [문제 배열]
      ├── timeLimit: 1200
      ├── updatedAt: "2025-01-15T10:30:00Z"
      └── count: 15
```

### responses 컬렉션

```
responses/
  └── {학번}/
      ├── name: "홍길동"
      ├── studentId: "20241234"
      ├── mode: "real"
      ├── answers: [{questionId, answer, points}, ...]
      ├── submittedAt: "2025-01-15T11:00:00Z"
      └── timeSpent: 1140 (초)
```

---

## 🧪 로컬 테스트

### Python 사용:

```bash
# Python 3
python -m http.server 8000

# 브라우저에서:
# http://localhost:8000/index.html (학생용)
# http://localhost:8000/admin.html (관리자용)
```

### Node.js 사용:

```bash
npx http-server -p 8000

# 브라우저에서:
# http://localhost:8000/index.html
# http://localhost:8000/admin.html
```

---

## 🚀 Firebase 배포

### 방법 1: Firebase Hosting

```bash
# Firebase CLI 설치
npm install -g firebase-tools

# 로그인
firebase login

# 초기화
firebase init hosting

# 배포
firebase deploy --only hosting
```

### 방법 2: Firebase Console 직접 업로드

1. Firebase Console → Hosting
2. "시작하기" 또는 "새 배포 추가"
3. index.html, admin.html 드래그
4. "업로드" 버튼 클릭

---

## 💡 사용 팁

### 교수님을 위한 워크플로우:

1. **준비** (5분)
   - CSV 파일 작성 (예제 템플릿 사용)
   - admin.html에서 시험 시간 설정

2. **업로드** (1분)
   - 연습 문제 CSV 업로드
   - 실전 문제 CSV 업로드
   - 업로드 확인

3. **공지** (1분)
   - 학생들에게 index.html URL 공유
   - 시험 일정 안내

4. **모니터링**
   - admin.html에서 "전체 응답 보기"
   - 실시간 제출 현황 확인

5. **채점**
   - Firebase Console에서 응답 확인
   - answerKey 참고하여 채점

---

## 🆘 문제 해결

### Q: "Firebase config not found" 오류
**A**: index.html과 admin.html의 Firebase 설정 확인

### Q: 문제가 로드되지 않음
**A**: 
1. admin.html에서 문제 업로드 확인
2. Firebase Console → Firestore에서 데이터 확인
3. 보안 규칙 확인

### Q: 학생이 재응시 불가
**A**: 실전 모드는 1회 제한. Firebase Console에서 해당 학번 문서 삭제 시 재응시 가능

### Q: CSV 업로드 실패
**A**: 
1. CSV 형식 확인 (8개 컬럼)
2. UTF-8 인코딩 확인
3. 템플릿 다운로드하여 참조

---

## ✅ 최종 체크리스트

배포 전 확인사항:

- [ ] index.html Firebase 설정 완료
- [ ] admin.html Firebase 설정 완료
- [ ] Firestore 보안 규칙 설정
- [ ] 로컬 테스트 성공
- [ ] CSV 파일 준비
- [ ] admin.html에서 문제 업로드 테스트
- [ ] index.html에서 시험 응시 테스트
- [ ] 시간 제한 작동 확인

---

## 🎉 완료!

이제 모든 준비가 완료되었습니다!

**학생용 URL**: https://your-project.web.app/index.html  
**관리자 URL**: https://your-project.web.app/admin.html

---

**교수님의 꿈이 현실이 되었습니다! 🚀✨**
