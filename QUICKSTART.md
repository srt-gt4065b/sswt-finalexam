# ⚡ 빠른 시작 가이드 (5분 완성!)

## 🎯 목표

5분 안에 시험 시스템을 완전히 설정하고 첫 시험을 준비합니다!

---

## 📋 1단계: Firebase 설정 (2분)

### A. Firebase Config 복사

1. 브라우저에서 열기: https://console.firebase.google.com/
2. 프로젝트 선택 (없으면 새로 만들기)
3. 왼쪽 상단 ⚙️ (톱니바퀴) → "프로젝트 설정"
4. 아래로 스크롤 → "내 앱" → 웹 앱 선택
5. "SDK 설정 및 구성" → "구성" 선택
6. **firebaseConfig 전체 복사**

### B. index.html 수정

**메모장이나 편집기로 index.html 열기**

약 30번째 줄 찾기 (Ctrl+F로 "YOUR_API_KEY" 검색):

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",           // ← 여기에 복사한 값 붙여넣기
    authDomain: "YOUR_PROJECT.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT.appspot.com",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

### C. admin.html 수정

**메모장이나 편집기로 admin.html 열기**

약 195번째 줄 찾기 (Ctrl+F로 "YOUR_API_KEY" 검색):

```javascript
// index.html과 동일한 설정 복사해서 붙여넣기
const firebaseConfig = {
    apiKey: "복사한_실제_값",
    authDomain: "복사한_실제_값",
    projectId: "복사한_실제_값",
    storageBucket: "복사한_실제_값",
    messagingSenderId: "복사한_실제_값",
    appId: "복사한_실제_값"
};
```

**저장!** (Ctrl+S)

---

## 🔓 2단계: Firestore 보안 규칙 (1분)

1. Firebase Console → Firestore Database
2. "규칙" 탭 클릭
3. 다음 코드로 교체:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 12, 31);
    }
  }
}
```

4. "게시" 버튼 클릭

---

## 📤 3단계: 문제 업로드 (2분)

### A. admin.html 열기

로컬에서:
```bash
# 폴더에서 admin.html 더블클릭
# 또는
python -m http.server 8000
# 브라우저에서 http://localhost:8000/admin.html
```

### B. 템플릿 다운로드

1. admin.html 하단 "예제 템플릿 다운로드" 클릭
2. question_template.csv 파일 저장

### C. CSV 파일 편집 (선택)

Excel이나 메모장으로 열어서 문제 수정 가능

### D. 업로드

1. **"연습 모드"** 탭 선택
2. "파일 선택" 클릭 → CSV 파일 선택
3. "문제 업로드" 클릭
4. ✅ 성공 메시지 확인!

### E. 시험 시간 설정

1. 시간 입력 (예: 10분)
2. "시간 저장" 클릭

---

## 🧪 4단계: 테스트 (30초)

### A. index.html 열기

```bash
# 폴더에서 index.html 더블클릭
```

### B. 시험 시작

1. 이름: 테스트
2. 학번: 12345
3. "연습 모드 시작" 클릭

### C. 확인사항

- ✅ 문제가 나타나는가?
- ✅ 타이머가 작동하는가?
- ✅ 답안 입력 가능한가?
- ✅ 제출 버튼이 보이는가?

**모두 ✅ 이면 완료!**

---

## 🎉 완료! 이제 사용 가능합니다!

### 학생들에게 공유:

**방법 1: 로컬 서버**
```
http://localhost:8000/index.html
```

**방법 2: Firebase 배포 후**
```
https://your-project.web.app/index.html
```

### 관리자 접속:

**방법 1: 로컬**
```
http://localhost:8000/admin.html
```

**방법 2: Firebase 배포 후**
```
https://your-project.web.app/admin.html
```

---

## 📚 다음 단계

### 실전 시험 준비:

1. admin.html → "실전 시험" 탭
2. 실전 문제 CSV 업로드
3. 시험 시간 설정 (예: 20분)
4. 학생들에게 URL 공유

### 응답 확인:

1. admin.html → 하단 "전체 응답 보기"
2. 제출된 답안 확인
3. 채점

---

## 🆘 문제 발생 시

### "Firebase config not found"
→ 1단계 다시 확인 (YOUR_API_KEY를 실제 값으로 교체했는지)

### "Permission denied"
→ 2단계 다시 확인 (Firestore 규칙 설정)

### 문제가 안 보임
→ 3단계 다시 확인 (CSV 업로드 성공했는지)

---

## ⏱️ 시간 요약

- Firebase 설정: 2분
- 보안 규칙: 1분
- 문제 업로드: 2분
- **총 5분!**

---

## 💡 꿀팁

### Excel로 문제 작성:

1. 템플릿 CSV 열기
2. Excel에서 편집
3. "CSV UTF-8(쉼표로 분리)"로 저장
4. 업로드!

### 변수 사용:

```
"당신의 평균 점수는 {avgScore}입니다."
```
→ 각 학생마다 다른 값으로 자동 치환!

### 차트 생성:

```csv
Q1,chart,"히스토그램을 보고...",20,"답",true,histogram,hard
```
→ 학생 데이터로 차트 자동 생성!

---

**5분 만에 완성! 🚀**

더 자세한 내용은 README.md를 참조하세요!
